<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Bookworm Main Page</h1>
    <nav>
        <a href="/bookworm/new">To add a new Book</a>
    </nav>
    <div class="library">
    <% for (let i = 0; i < book.length; i++) { %>
        <div class="LibraryItem">
            <div class="book">
                <h4><%=book[i].name%></h4>
            </div>
                <a href="/bookworm/<%=book.id%>">
                <img class="bookImage" src="<%= `${book[i].img}`%>">
                </a>
            <div class="editAndDelete">
                <button><a href="/bookworm/<%=book.id%>/edit">Edit</a></button>
                <form action="/bookworm/<%=book.id%>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE" />
                </form>
            </div>
        </div>
    <% }%>
    </div>

</body>
</html>